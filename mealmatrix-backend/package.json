{
  "name": "mealmatrix-backend",
  "version": "2.0.0",
  "description": "MealMatrix advanced backend — order state machine, idempotent checkout, geo-indexed restaurants",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "jest": "^29.7.0",
    "mongodb-memory-server": "^9.2.0"
  }
}
